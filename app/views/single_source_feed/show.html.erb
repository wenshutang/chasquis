<title> La Razón feed </title>

<div id="recent_episodes">
  <h3>La Razón feed</h3>
  <ul>
    <% for feed in @feed_list %>
        <li><%= link_to h(feed.name), feed.url %></li>
    <% end %>
  </ul>
</div>